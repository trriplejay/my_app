language: node_js

node_js:
  - 0.12
  
build:

  pre_ci:
    - echo "pre ci section"
    
  ci:
  
    - echo "versionName=$BRANCH.$BUILD_NUMBER" >> $MY_APP_RUNCI_STATE/my_image.env
    
  push: 
    - echo "push section"
    
