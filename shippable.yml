language: node_js

node_js:
  - 0.12

build:

  ci:
    - echo "buidl and push image" 

integrations:
  notification:
    - name: "trigger_my_image"
      type: webhook
      payload:
        - versionName: $BRANCH.$BUILD_NUMBER
      
